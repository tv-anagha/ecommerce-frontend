# User microservice (no trailing slash). Default port 8082.
# Dev with Vite proxy (no CORS): leave empty
VITE_API_BASE_URL=

# Direct call (enable CORS on user-service):
# VITE_API_BASE_URL=http://localhost:8082

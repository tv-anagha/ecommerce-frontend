# Product microservice (no trailing slash)
# Dev with Vite proxy (recommended — no CORS): leave empty
VITE_API_BASE_URL=

# Direct call (backend must allow CORS from http://localhost:5173):
# VITE_API_BASE_URL=http://localhost:8081
